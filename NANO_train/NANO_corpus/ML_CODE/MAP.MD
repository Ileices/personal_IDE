```
if self branch "from" = child/sub of/folder [create sub folder for self as the parent launches/orchestrates self]
if self branch "to" = parent of/folder [if parent then create one child folder. in child folder place scripts parent will launch/orchestrate in overall loop. The 3 child scrips each create one child folder to branch into with 3 scripts to launch/orchestrate.] 
if in child or parent folder then all scripts must function as one program

Ileices.py should be the orchestrator and launch of the entire who what when where why how structure.
Ileices.py should be created in the Ileices folder. 
Ileices folder should be the parent of folders who, what, when, where, why, how, and structure folders.
Ileices.py should be the only script in its directory all other scripts should be in proper subfolders.
All code outputs should be output in the exact folder location as the script producing it in the codebase.
The system should work on windows and linux.
This is a real time deep learning and inference chatbot system that "lives" in the computer and the  


┌─────────────────────────────────────────────────────────────────┐
│                              WHAT?                              │
├─────────────────────────────────────────────────────────────────┤
│ UNIFIED_ABSOLUTE_FRAMEWORK_COMPLETE_TECHNICAL_OVERVIEW.md       │
│ unified_theory_overview.md                                      │
| AE_Math.md                                                      |
| AE_Equations_Master.txt                                         |
│ A Recursive Computational Cosmology for Conscious Machines.txt  │
| Nothing_Fake_Allowed.md                                         |
|[read-only files that guide LLM to build the real function.]     |
├─────────────────────────────────────────────────────────────────┤
| ABOVE FILES TRANSLATE TO THE ACTUAL FUNCTIONING FILES WE WILL   |
| USE. MFOF WILL BE BUILT TO ADHERER TO THE FILES ABOVE.          | 
├────────────────────────────────────────────────────────────────|||────────────────────────────────────────────────────────────────┐
│ [master framework orchestrator file (mfof)]                                                                                       |
│ mfof is the script that launches all subsystems of "WHAT?" tab                                                                    |
│                                                                                                                                   |
├───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ mfof creates 3 children folders (r, b, y) and places one script in each folder:                                                   |
│ r.py in r/ folder - handles red spectrum framework operations - references ATTACK folder algorithms                               |
│ b.py in b/ folder - handles blue spectrum framework operations - references ATTACK folder algorithms                              |
│ y.py in y/ folder - handles yellow spectrum framework operations - references ATTACK folder algorithms                            |
│                                                                                                                                   |
│ Each first-generation child (r, b, y) then creates its own 3 children:                                                            |
│ r.py creates r/ folder with r1.py, b1.py, y1.py scripts                                                                           |
│ b.py creates b/ folder with r1.py, b1.py, y1.py scripts                                                                           |  
│ y.py creates y/ folder with r1.py, b1.py, y1.py scripts                                                                           |
│                                                                                                                                   |
│ Each second-generation child creates its own 3 children using lazy instantiation:                                                 |
│ Virtual generation continues infinitely but only materializes when called                                                         |
│ Each script is responsible for launching/orchestrating exactly 3 direct children                                                  |
│ This creates a fractal tree structure ensuring cohesive codebase operation                                                        |
├───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
 \  the folder ATTACK has code that has real algorithms we will need to harvest. the ATTACK folder is READ-ONLY during project     /
  \ Development. when building and running tests they must be isolated tests it must be on the actual project script being tested /
   \ and MUST NOT be named "test or demo or demonstration" there will only be actual finished scripts in this project and no     /
    \ ISLAND programming. if a script is added then it must have an exact reason and a report written on it. reports should be  /
     \ written in the codebase_audit folder. the reports MUST be structured in parent and sub folders exactly how their code   /
      \ is structured that it is describing. each report must tell exactly how it fits into the above tree and exactly its    /
       \ steps all the way to the master orchestrator and through each orchestrator it is connected by through the entire    /
        \ tree of code to ensure no island scripts exist. The master project will be in the Ileices folder.                 /
         \_________________________________________________________________________________________________________________/
                 


┌─────────────────────────────────────────────────────────────────┐
│                         WHO? = Ileices                          │
├─────────────────────────────────────────────────────────────────┤
│ ORGANISM_AE_BLUEPRINTS_Comprehensive_Analysis.md                │
| embrionic_scripts.txt                                           |
| ORGANISM AE BLUEPRINTS.txt                                      |
| Theory of Absolute Perception.txt                               |
| The Theory Of Absolute Focus.txt                                |
| The Theory of Absolute Thought.txt                              |
| A Recursive Computational Cosmology for Conscious Machines.txt  |
| Nothing_Fake_Allowed.md                                         |
|[read-only files that guide LLM to build the real function.]     |
├─────────────────────────────────────────────────────────────────┤
| ABOVE FILES TRANSLATE TO THE ACTUAL FUNCTIONING FILES WE WILL   |
| USE. MIOF WILL BE BUILT TO ADHERER TO THE FILES ABOVE.          | 
├────────────────────────────────────────────────────────────────|||────────────────────────────────────────────────────────────────┐
│ [master identity orchestrator file (miof)]                                                                                        |
│ miof is the script that launches all subsystems of "WHO?"                                                                         |
│                                                                                                                                   |
├───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ miof creates 3 children folders (r, b, y) and places one script in each folder:                                                   |
│ r.py in r/ folder - handles red spectrum identity operations - references ATTACK folder algorithms                                |
│ b.py in b/ folder - handles blue spectrum identity operations - references ATTACK folder algorithms                               |
│ y.py in y/ folder - handles yellow spectrum identity operations - references ATTACK folder algorithms                            |
│                                                                                                                                   |
│ Each first-generation child (r, b, y) then creates its own 3 children:                                                           |
│ r.py creates r/ folder with r1.py, b1.py, y1.py scripts                                                                          |
│ b.py creates b/ folder with r1.py, b1.py, y1.py scripts                                                                          |  
│ y.py creates y/ folder with r1.py, b1.py, y1.py scripts                                                                          |
│                                                                                                                                   |
│ Each second-generation child creates its own 3 children using lazy instantiation:                                                |
│ Virtual generation continues infinitely but only materializes when called                                                         |
│ Each script is responsible for launching/orchestrating exactly 3 direct children                                                 |
│ This creates a fractal tree structure ensuring cohesive codebase operation                                                       |
├───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
 \  the folder ATTACK has code that has real algorithms we will need to harvest. the ATTACK folder is READ-ONLY during project     /
    \ Development. when building and running tests they must be isolated tests it must be on the actual project script being tested /
     \ and MUST NOT be named "test or demo or demonstration" there will only be actual finished scripts in this project and no     /
        \ ISLAND programming. if a script is added then it must have an exact reason and a report written on it. reports should be  /
         \ written in the codebase_audit folder. the reports MUST be structured in parent and sub folders exactly how their code   /
            \ is structured that it is describing. each report must tell exactly how it fits into the above tree and exactly its    /
             \ steps all the way to the master orchestrator and through each orchestrator it is connected by through the entire    /
                \ tree of code to ensure no island scripts exist. The master project will be in the Ileices folder.                 /
                 \_________________________________________________________________________________________________________________/
                 


┌─────────────────────────────────────────────────────────────────┐
│                             WHERE?                              │
├─────────────────────────────────────────────────────────────────┤
│ 9ixel_Complete_Computer_Science_Analysis.md                     │
│ 9ixel_comprehensive_overview.md                                 │
│ 9ixel_Digital_Universe_Complete_Analysis.md                     │
│ 9pixel_comprehensive_analysis.md                                │
│ 9pixel_comprehensive_overview.md                                │
| Nothing_Fake_Allowed.md                                         |
| Absolute Existence Theory Underlying story of the game.txt      |
| [read-only files that guide LLM to build the real function.]    |
├─────────────────────────────────────────────────────────────────┤
| ABOVE FILES TRANSLATE TO THE ACTUAL FUNCTIONING FILES WE WILL   |
| USE. MWOF WILL BE BUILT TO ADHERER TO THE FILES ABOVE.          | 
├────────────────────────────────────────────────────────────────|||────────────────────────────────────────────────────────────────┐
│ [master world orchestrator file (mwof)]                                                                                           |
│ mwof is the script that launches all subsystems of "WHERE?"                                                                       |
│                                                                                                                                   |
├───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ mwof creates 3 children folders (r, b, y) and places one script in each folder:                                                   |
│ r.py in r/ folder - handles red spectrum spatial operations - references ATTACK folder algorithms                                 |
│ b.py in b/ folder - handles blue spectrum spatial operations - references ATTACK folder algorithms                                |
│ y.py in y/ folder - handles yellow spectrum spatial operations - references ATTACK folder algorithms                             |
│                                                                                                                                   |
│ Each first-generation child (r, b, y) then creates its own 3 children:                                                           |
│ r.py creates r/ folder with r1.py, b1.py, y1.py scripts                                                                          |
│ b.py creates b/ folder with r1.py, b1.py, y1.py scripts                                                                          |  
│ y.py creates y/ folder with r1.py, b1.py, y1.py scripts                                                                          |
│                                                                                                                                   |
│ Each second-generation child creates its own 3 children using lazy instantiation:                                                |
│ Virtual generation continues infinitely but only materializes when called                                                         |
│ Each script is responsible for launching/orchestrating exactly 3 direct children                                                 |
│ This creates a fractal tree structure ensuring cohesive codebase operation                                                       |
├───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
 \  the folder ATTACK has code that has real algorithms we will need to harvest. the ATTACK folder is READ-ONLY during project     /
    \ Development. when building and running tests they must be isolated tests it must be on the actual project script being tested /
     \ and MUST NOT be named "test or demo or demonstration" there will only be actual finished scripts in this project and no     /
        \ ISLAND programming. if a script is added then it must have an exact reason and a report written on it. reports should be  /
         \ written in the codebase_audit folder. the reports MUST be structured in parent and sub folders exactly how their code   /
            \ is structured that it is describing. each report must tell exactly how it fits into the above tree and exactly its    /
             \ steps all the way to the master orchestrator and through each orchestrator it is connected by through the entire    /
                \ tree of code to ensure no island scripts exist. The master project will be in the Ileices folder.                 /
                 \_________________________________________________________________________________________________________________/
                 

┌─────────────────────────────────────────────────────────────────┐
│                              HOW?                               │
├─────────────────────────────────────────────────────────────────┤
│ computational_overview.md                                       │
│ Chat_Singularity_Complete_System_Overview.md                    │
│ Chat_Singularity_ULTIMATE_Complete_Analysis.md                  │
| ADVANCED_VISUALIZATION_ANALYSIS.md                              |
| PTAIE.md                                                        |
│ AIOS IO Requirements to mimic chatGPT.txt                       │
| The Theory of Absolute Precision.txt                            |
| Theory of Absolute Photonic-Evolutionary Symbiosis and Memory in DNA.txt |
| paste on top of aeos_io_unified.py.txt                          |
| NLP to code.txt                                                 |
| Chat Singularity_AI.txt                                         |
| Chat Singularity_user.txt                                       |
| Nothing_Fake_Allowed.md                                         |
| A Recursive Computational Cosmology for Conscious Machines.txt  |
| The Theory of Absolute Color.py - read-only for reference enhancement |
| weirdAI.md                                                      |
|[read-only files that guide LLM to build the real function.]     |
├─────────────────────────────────────────────────────────────────┤
| ABOVE FILES TRANSLATE TO THE ACTUAL FUNCTIONING FILES WE WILL   |
| USE. MLOF WILL BE BUILT TO ADHERER TO THE FILES ABOVE.          | 
├────────────────────────────────────────────────────────────────|||────────────────────────────────────────────────────────────────┐
│ [master laws orchestrator file (mlof)]                                                                                            |
│ mlof is the script that launches all subsystems of "HOW?"                                                                         |
│                                                                                                                                   |
├───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ mlof creates 3 children folders (r, b, y) and places one script in each folder:                                                   |
│ r.py in r/ folder - handles red spectrum methods operations - references ATTACK folder algorithms                                 |
│ b.py in b/ folder - handles blue spectrum methods operations - references ATTACK folder algorithms                                |
│ y.py in y/ folder - handles yellow spectrum methods operations - references ATTACK folder algorithms                             |
│                                                                                                                                   |
│ Each first-generation child (r, b, y) then creates its own 3 children:                                                           |
│ r.py creates r/ folder with r1.py, b1.py, y1.py scripts                                                                          |
│ b.py creates b/ folder with r1.py, b1.py, y1.py scripts                                                                          |  
│ y.py creates y/ folder with r1.py, b1.py, y1.py scripts                                                                          |
│                                                                                                                                   |
│ Each second-generation child creates its own 3 children using lazy instantiation:                                                |
│ Virtual generation continues infinitely but only materializes when called                                                         |
│ Each script is responsible for launching/orchestrating exactly 3 direct children                                                 |
│ This creates a fractal tree structure ensuring cohesive codebase operation                                                       |
├───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
 \  the folder ATTACK has code that has real algorithms we will need to harvest. the ATTACK folder is READ-ONLY during project     /
    \ Development. when building and running tests they must be isolated tests it must be on the actual project script being tested /
     \ and MUST NOT be named "test or demo or demonstration" there will only be actual finished scripts in this project and no     /
        \ ISLAND programming. if a script is added then it must have an exact reason and a report written on it. reports should be  /
         \ written in the codebase_audit folder. the reports MUST be structured in parent and sub folders exactly how their code   /
            \ is structured that it is describing. each report must tell exactly how it fits into the above tree and exactly its    /
             \ steps all the way to the master orchestrator and through each orchestrator it is connected by through the entire    /
                \ tree of code to ensure no island scripts exist. The master project will be in the Ileices folder.                 /
                 \_________________________________________________________________________________________________________________/
                 


┌─────────────────────────────────────────────────────────────────┐
│                              WHY?                               │
├─────────────────────────────────────────────────────────────────┤
│ NLP_to_Code_Doctoral_Analysis.md                                │
│ NLP_to_Code_Methodology_Overview.md                             │
│ Decoder_Engine_Singularity_Comprehensive_Analysis.md            │
| NLP to code.txt                                                 |
| Chat Singularity_user.txt                                       |
|[read-only files that guide LLM to build the real function.]     |
├─────────────────────────────────────────────────────────────────┤
| ABOVE FILES TRANSLATE TO THE ACTUAL FUNCTIONING FILES WE WILL   |
| USE. MPOF WILL BE BUILT TO ADHERER TO THE FILES ABOVE.          | 
├────────────────────────────────────────────────────────────────|||────────────────────────────────────────────────────────────────┐
│ [master purpose orchestrator file (mpof)]                                                                                         |
│ mpof is the script that launches all subsystems of "WHY?"                                                                         |
│                                                                                                                                   |
├───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ mpof creates 3 children folders (r, b, y) and places one script in each folder:                                                   |
│ r.py in r/ folder - handles red spectrum purpose operations - references ATTACK folder algorithms                                 |
│ b.py in b/ folder - handles blue spectrum purpose operations - references ATTACK folder algorithms                                |
│ y.py in y/ folder - handles yellow spectrum purpose operations - references ATTACK folder algorithms                             |
│                                                                                                                                   |
│ Each first-generation child (r, b, y) then creates its own 3 children:                                                           |
│ r.py creates r/ folder with r1.py, b1.py, y1.py scripts                                                                          |
│ b.py creates b/ folder with r1.py, b1.py, y1.py scripts                                                                          |  
│ y.py creates y/ folder with r1.py, b1.py, y1.py scripts                                                                          |
│                                                                                                                                   |
│ Each second-generation child creates its own 3 children using lazy instantiation:                                                |
│ Virtual generation continues infinitely but only materializes when called                                                         |
│ Each script is responsible for launching/orchestrating exactly 3 direct children                                                 |
│ This creates a fractal tree structure ensuring cohesive codebase operation                                                       |
├───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
 \  the folder ATTACK has code that has real algorithms we will need to harvest. the ATTACK folder is READ-ONLY during project     /
    \ Development. when building and running tests they must be isolated tests it must be on the actual project script being tested /
     \ and MUST NOT be named "test or demo or demonstration" there will only be actual finished scripts in this project and no     /
        \ ISLAND programming. if a script is added then it must have an exact reason and a report written on it. reports should be  /
         \ written in the codebase_audit folder. the reports MUST be structured in parent and sub folders exactly how their code   /
            \ is structured that it is describing. each report must tell exactly how it fits into the above tree and exactly its    /
             \ steps all the way to the master orchestrator and through each orchestrator it is connected by through the entire    /
                \ tree of code to ensure no island scripts exist. The master project will be in the Ileices folder.                 /
                 \_________________________________________________________________________________________________________________/
                 



┌─────────────────────────────────────────────────────────────────┐
│                           STRUCTURE?                            │
├─────────────────────────────────────────────────────────────────┤
│ weird_examined.md + weird.md                                    │
│ weirdAI_examined.md + weirdAI.md                                |
| The Problem of Absolute Position A Foundational Analysis within the Theory of Absolute Existence.txt  |
| Nothing_Fake_Allowed.md                                         | 
| Absolute Existence Theory Underlying story of the game.txt      |
| Chat Singularity_AI.txt                                         |
| ADVANCED_VISUALIZATION_ANALYSIS.md                              |
| Absolute Theory of Black Holes and the Absularis to Absularity.txt |
|[read-only files that guide LLM to build the real function.]     |
├─────────────────────────────────────────────────────────────────┤
| ABOVE FILES TRANSLATE TO THE ACTUAL FUNCTIONING FILES WE WILL   |
| USE. MSOF WILL BE BUILT TO ADHERER TO THE FILES ABOVE.          |
├────────────────────────────────────────────────────────────────|||────────────────────────────────────────────────────────────────┐
│ [master structure orchestrator file (msof)]                                                                                       |
│ msof is the script that launches all subsystems of "STRUCTURE?"                                                                   |
│                                                                                                                                   |
├───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ msof creates 3 children folders (r, b, y) and places one script in each folder:                                                   |
│ r.py in r/ folder - handles red spectrum structure operations - references ATTACK folder algorithms                               |
│ b.py in b/ folder - handles blue spectrum structure operations - references ATTACK folder algorithms                              |
│ y.py in y/ folder - handles yellow spectrum structure operations - references ATTACK folder algorithms                           |
│                                                                                                                                   |
│ Each first-generation child (r, b, y) then creates its own 3 children:                                                           |
│ r.py creates r/ folder with r1.py, b1.py, y1.py scripts                                                                          |
│ b.py creates b/ folder with r1.py, b1.py, y1.py scripts                                                                          |  
│ y.py creates y/ folder with r1.py, b1.py, y1.py scripts                                                                          |
│                                                                                                                                   |
│ Each second-generation child creates its own 3 children using lazy instantiation:                                                |
│ Virtual generation continues infinitely but only materializes when called                                                         |
│ Each script is responsible for launching/orchestrating exactly 3 direct children                                                 |
│ This creates a fractal tree structure ensuring cohesive codebase operation                                                       |
├───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
 \  the folder ATTACK has code that has real algorithms we will need to harvest. the ATTACK folder is READ-ONLY during project     /
    \ Development. when building and running tests they must be isolated tests it must be on the actual project script being tested /
     \ and MUST NOT be named "test or demo or demonstration" there will only be actual finished scripts in this project and no     /
        \ ISLAND programming. if a script is added then it must have an exact reason and a report written on it. reports should be  /
         \ written in the codebase_audit folder. the reports MUST be structured in parent and sub folders exactly how their code   /
            \ is structured that it is describing. each report must tell exactly how it fits into the above tree and exactly its    /
             \ steps all the way to the master orchestrator and through each orchestrator it is connected by through the entire    /
                \ tree of code to ensure no island scripts exist. The master project will be in the Ileices folder.                 /
                 \_________________________________________________________________________________________________________________/
                 




┌─────────────────────────────────────────────────────────────────┐
│                              WHEN?                              │
├─────────────────────────────────────────────────────────────────┤
│ 24/7 Operation Framework Embed In All Systems                   │
│ weirdAI.md weird.md                                             │
│ Nothing_Fake_Allowed.md                                         │
|[read-only files that guide LLM to build the real function.]     |
├─────────────────────────────────────────────────────────────────┤
│ ABOVE FILES TRANSLATE TO THE ACTUAL FUNCTIONING FILES WE WILL   │
│ USE. MTOF WILL BE BUILT TO ADHERER TO THE FILES ABOVE.          │

├────────────────────────────────────────────────────────────────|||────────────────────────────────────────────────────────────────┐
│ [master time orchestrator file (mtof)]                                                                                            │
│ mtof is the script that launches all subsystems of "WHEN?"                                                                        │
│                                                                                                                                   │
├───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ mtof creates 3 children folders (r, b, y) and places one script in each folder:                                                   │
│ r.py in r/ folder - handles red spectrum temporal operations - references ATTACK folder algorithms                                │
│ b.py in b/ folder - handles blue spectrum temporal operations - references ATTACK folder algorithms                               │
│ y.py in y/ folder - handles yellow spectrum temporal operations - references ATTACK folder algorithms                            │
│                                                                                                                                   │
│ Each first-generation child (r, b, y) then creates its own 3 children:                                                           │
│ r.py creates r/ folder with r1.py, b1.py, y1.py scripts                                                                          │
│ b.py creates b/ folder with r1.py, b1.py, y1.py scripts                                                                          │  
│ y.py creates y/ folder with r1.py, b1.py, y1.py scripts                                                                          │
│                                                                                                                                   │
│ Each second-generation child creates its own 3 children using lazy instantiation:                                                │
│ Virtual generation continues infinitely but only materializes when called                                                         │
│ Each script is responsible for launching/orchestrating exactly 3 direct children                                                 │
│ This creates a fractal tree structure ensuring cohesive codebase operation                                                       │
├───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
 \  the folder ATTACK has code that has real algorithms we will need to harvest. the ATTACK folder is READ-ONLY during project     /
    \ Development. when building and running tests they must be isolated tests it must be on the actual project script being tested /
     \ and MUST NOT be named "test or demo or demonstration" there will only be actual finished scripts in this project and no     /
        \ ISLAND programming. if a script is added then it must have an exact reason and a report written on it. reports should be  /
         \ written in the codebase_audit folder. the reports MUST be structured in parent and sub folders exactly how their code   /
            \ is structured that it is describing. each report must tell exactly how it fits into the above tree and exactly its    /
             \ steps all the way to the master orchestrator and through each orchestrator it is connected by through the entire    /
                \ tree of code to ensure no island scripts exist. The master project will be in the Ileices folder.                 /
                 \_________________________________________________________________________________________________________________/
                 


    
