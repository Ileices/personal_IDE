AIOS_IO kernel - quick start

Run the GUI (requires a Python environment with dependencies in `requirements.txt`):

```powershell
python -m pip install -r requirements.txt
python kernel\gui.py
```

If Multipass is installed, the GUI will enable VM controls. If Docker is installed, Docker-related helpers may be available.
